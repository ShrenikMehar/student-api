replicaCount: 1

image:
  repository: shrenikmehar/student-api
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 8080
  nodePort: 30080

nodeSelector:
  type: application

config:
  dbHost: postgres
  dbPort: 5432
  dbName: students

secret:
  dbUser: postgres
  dbPassword: postgres
